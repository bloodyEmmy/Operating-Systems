#flashcards 
***
Стандартная [[Библиотечная функция|библиотечная функция]] языка Си, которая определена в `<time.h>` и возвращает текущее системное время в секундах, прошедших с "эпохи [[Unix]]" (т.е. 01.01.1970).
***
***Синтаксис.***
```C
#include <time.h>

time_t time(time_t *tloc);
```
